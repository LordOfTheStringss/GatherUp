# Gather Up - LLD Implementation Tasks

## Phase 1: Core Domain Models (Data Structures & Interfaces)
- [x] Create directory structure and empty class files.
- [x] Implement `core.identity` (User, AuthManager, FriendshipManager, GamificationManager).
- [x] Implement `core.identity` (User, AuthManager, FriendshipManager, GamificationManager).
- [x] Implement `core.event` (Event, EventBuilder, ConflictEngine, ChatRoom, Message, SafetyService).
- [x] Implement `core.schedule` (TimeSlot, Schedule, OCRProcessor).

## Phase 2: Intelligence & Spatial (AI & Mapping)
- [x] Implement `spatial` (Location, RegionAnalyzer).
- [x] Implement `intelligence` (VectorService, RecommendationEngine, MatchingService).

## Phase 3: Infrastructure & Admin (External Services & Governance)
- [x] Implement `infra` (SupabaseClient, NotificationService).
- [x] Implement `admin.moderation` & `admin.governance` & `admin.analytics`.

## Phase 4: Controllers & API Gateway
- [x] Implement `controllers` (AuthController, UserController, EventController, ScheduleController, AdminController).
- [x] Refine controllers to match perfect LLD property/method signatures.
- [x] Create missing Manager stubs (UserManager, EventManager, ScheduleManager).

## Phase 5: UI Integration (Expo App)
- [x] Setup Zustand store and global UI components (Loaders, Toasts).
- [x] Implement `app/(auth)`: Sign-Up/Login with Domain Validation.
- [x] Implement `app/(auth)`: Interest Selection (Swipe-to-Select).
- [x] Implement `app/(auth)`: Plan Your Time (OCR + Grid UI).
- [x] Implement `app/(tabs)/index`: Home Dashboard ("Suggest for Me", Feeds, FAB).
- [x] Implement `app/(tabs)/create`: 3-Step Wizard & Group AI Planning.
- [x] Implement `app/(tabs)/map`: Spatio-Temporal Map & Heatmap.
- [x] Implement `app/event/[id]`: Chat Room & Badge Economy popup.
- [x] Implement global Safety/Panic Button.

## Phase 6: UI Refinements & TypeScript Fixes
- [x] Fix TypeScript compilation/lint errors in Expo app components.
- [x] Enhance aesthetics of the "Plan with AI" tab in `create.tsx`.
- [x] Implement real Swipeable (PanResponder) Interests Onboarding.
- [x] Implement list-based `edit-interests.tsx` accessible from Profile.

## Phase 7: App Polish & Functional Completeness
- [x] Fix `interests.tsx` PanResponder closure bug.
- [x] App layout revamp: central "Suggest" tab button in `_layout.tsx`.
- [x] Redesign Home Dashboard (`index.tsx`) for premium aesthetics and detailed cards.
- [x] Complete Event Creation (`create.tsx`) with category chips.
- [x] Populate Profile (`profile.tsx`) with realistic user stats and actions.
- [x] Implement real interactive map with `react-native-maps` in `map.tsx`.
- [x] Create `edit-profile.tsx` with `expo-image-picker` for profile picture uploads.
- [x] Add "Available / Busy" toggle in `profile.tsx` to control AI suggestion visibility.

## Phase 8: Real Data Integration & UI Polish (Rounds 3-6)
- [x] **Bug Fix:** Resolve Supabase ambiguous relation error (`PGRST201`) in `EventManager.ts`.
- [x] **Location Integration:** Add exact map-based coordinate selection (`lat/lng`) to Event Builder.
- [x] **Map Zero States:** Fetch live coordinates for the map and display smart zero-state messages.
- [x] **My Events Section:** Filter and explicitly separate events created by the logged-in user in feeds.
- [x] **Category Filter Fix:** Stop literal `'All'` string match from hiding events.
- [x] **Segmented Feed:** Replace horizontal lists with a sticky vertical segmented control (Friends/My Events/Nearby).
- [x] **Chat Realism:** Remove mock placeholder messages (`user-2`) from Chat room on mount.
- [x] **UI Perfection:** Hide Expo Router default stack headers leaking over custom app headers.
